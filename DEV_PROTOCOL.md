# /DEV_PROTOCOL.md

## TOBO 프로젝트 검증 프로토콜 (영구 적용)

이 파일은 모든 작업 보고에 예외 없이 적용된다.

1. "완료했다"는 보고에는 반드시 실제 git commit hash 
   (git rev-parse HEAD 전체 출력, 지어내지 말 것)를 포함한다.
2. 테스트 통과 주장은 반드시 GitHub Actions 실행 링크로만 
   증명한다. 콘솔 텍스트를 붙여넣는 것으로 대체하지 않는다.
3. 확인 안 된 것은 "미구현/확인 안 됨"이라고 답한다. 
   이건 감점이 아니다.
4. 코드를 수정한 뒤에는 CI가 통과(초록불)할 때까지 
   "완료"로 보고하지 않는다.
5. 하드코딩된 값, 템플릿 기반 mock 데이터를 실제 검증 
   결과처럼 제시하지 않는다.
6. 이 프로토콜을 매 작업마다 다시 안내받지 않아도 
   스스로 적용한다. 어긴 이력이 발견되면 그 즉시 
   왜 어겼는지 원인을 규명해서 보고한다.

## 과거 발견된 회귀 버그 (재발 방지 대상)
- 지명/상호명 하드코딩 (하단, 사하구, 뽀송펫 등)
- 신뢰도 분기 OR/AND 조건 오류
- isSupported 플래그 하드코딩
- 카테고리 0건 시 타 업종 오매칭
- 프론트엔드/초기 카드 하드코딩 데이터와 백엔드 LLM 프롬프트(extractSlots) 스키마 불일치로 인한 오작동 (예: 범용 카테고리가 남아있어 UNSUPPORTED 처리됨)
